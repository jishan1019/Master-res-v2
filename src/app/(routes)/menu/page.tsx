import Header from "../(home)/header";
import Menu from "./menu";

export default function Page() {
          return (
                    <Header>
                              <Menu />
                    </Header>
          )
}