import { siteDescription, siteFullTitle, siteLogo, siteTitle } from '@/config/site-config';

export const Config = {
          siteLogo,
          siteTitle,
          siteFullTitle,
          siteDescription,
}
