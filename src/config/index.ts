import { baseUrl, nodeEnv, storeEncryptKey } from '@/config/dotenv-config';
import { address, contact, cookieName, currency, description, discount, fullTitle, isRestaurantOpen, keywords, logo, logoWhite, mdfLogo, mdfLogoWhite, openingHours, openTimes, tagLine, tagLine2, title, userAvatar } from '@/config/site-config';

export const Config = {
  nodeEnv,
  cookieName,
  logo,
  logoWhite,
  mdfLogo,
  mdfLogoWhite,
  title,
  fullTitle,
  tagLine,
  tagLine2,
  description,
  keywords,
  address,
  contact,
  openingHours,
  baseUrl,
  storeEncryptKey,
  userAvatar,
  openTimes,
  currency,
  isRestaurantOpen,
  discount,
};
